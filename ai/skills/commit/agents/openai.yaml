interface:
  display_name: "commit"
  short_description: "Format commits and record human-agent attribution"
  default_prompt: "Use $commit to create a scope-type-description formatted commit with human committer, agent author, and conventional Git trailers."
